spring.application.name=employee_management
#Database connection settings
spring.datasource.url=jdbc:mysql://localhost:3306/employee_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Hamroush@mysql_122775

# JPA / Hibernate Configuration
# validate ensures that Hibernate checks our Java Classes match our flyway SQL tables exactly
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# flyway configuration
spring.flyway.baseline-on-migrate=true


